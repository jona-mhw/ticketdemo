# Clave secreta para la aplicacion Flask
SECRET_KEY=tu-clave-secreta-aqui

# URL de conexion a la base de datos
# Ejemplo para SQLite (desarrollo local): DATABASE_URL="sqlite:///tickethome.db"
# Ejemplo para PostgreSQL (produccion): DATABASE_URL="postgresql://user:password@host:port/database"
DATABASE_URL=""
